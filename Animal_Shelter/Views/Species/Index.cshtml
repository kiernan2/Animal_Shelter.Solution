@using Animal_Shelter.Models;

<h1>Species</h1>

@if (@Model.Count == 0)
{
  <h3>No species have been added yet!</h3>
}

@foreach (var item in collection)
{
  <h3><a href="/categories/@category.Id">@category.Name</a></h3>
}